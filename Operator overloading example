#include <iostream>
using namespace std;

class Box {
public:
    int value;
    Box(int v) { value = v; }

    Box operator+(Box b) {
        return Box(value + b.value);
    }
};

int main() {
    Box b1(10), b2(20);
    Box b3 = b1 + b2;

    cout << b3.value;   // 30
}
